#include<stdio.h>
int main()
{
    int n,i,m,p=1,q=0;
    scanf("%d",&n);
    int values[n];

    for(i=1;i<=n;i++){
        scanf("%d",&values[m]);
    }
    for(i=1;i<=n;i++){
    if(values[m]==p){
        printf("Hard");
    }
    }
    return 0;
}
